<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Склад{% endblock %}</title>
    {% block stylesheets %}{% endblock %}
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">

    <!-- Optional theme -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap-theme.min.css" integrity="sha384-rHyoN1iRsVXV4nD0JutlnGaslCJuC7uwjduW9SVrLvRYooPp2bWYgmgJQIXwl/Sp" crossorigin="anonymous">

    <script type="text/javascript" src="//ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>

    <!-- Latest compiled and minified JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>
</head>
<body>
{% block header %}
    <nav class="navbar navbar-default">
        <div class="container-fluid" style="background-color: #505050">
            <label class="navbar-header">
                {% if app.user %}
                    <a href="{{ path('news_index') }}" class="navbar-brand"><p style="color: #FFFFFF">Главная</p></a>
                    <a class="navbar-brand" href="{{ path('user_show', {'id': app.user.id}) }}"><p style="color: #FFE484">{{ app.user.login }}</p></a>
                {% else %}
                    <a class="navbar-brand"><p style="color: #FFFFFF">NewNews</p></a>
                {% endif %}
            </label>

            <div>
                <ul class="nav navbar-nav">
                    {% if app.user %}
                        {% if app.user.information %}
                            <li><a style="color: lightgray"><b>Курс валют:</b></a></li>
                            {% if app.user.information.usd %}
                                <li>
                                    <a style="color: lightgray">
                                        USD - {{ app.user.information.usd }}
                                    </a>
                                </li>
                            {% endif %}

                            {% if app.user.information.eur %}
                            <li>
                                <a style="color: lightgray">
                                   EUR - {{ app.user.information.eur }}
                                </a>
                            </li>
                            {% endif %}
                        {% endif %}
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}

<div class="container-fluid">
    {% block body %}{% endblock %}
</div>
{% block javascripts %}{% endblock %}
</body>
</html>
