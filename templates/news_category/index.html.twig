{% extends 'base.html.twig' %}

{% block title %}Список категорий{% endblock %}

{% block body %}
    <h1>Список категорий</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Название</th>
                <th>Английское название</th>
            </tr>
        </thead>
        <tbody>
        {% for news_category in news_categories %}
            <tr>
                <td><a href="{{ path('news_category_show', {'id': news_category.id}) }}">{{ news_category.name }}</a></td>
                <td>{{ news_category.englishName }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Записей не найдено</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('news_category_new') }}">Добавить</a>
{% endblock %}
